import java.text.*;
import java.util.*;

public class movlangCustomListener extends movlangBaseListener{

	HashMap<String, > pointsToSet = new HashMap();
	
	@Override
	public void exitMov1(movlangParser.Mov1Context ctx) {
		if(ctx.REG() != null){
      			System.out.println("Move " + ctx.REG().get(1).getText() + " to " + ctx.REG().get(0).getText());
    		}
		else {
			System.out.println("Mislukt");
		}
	}
}
