HashMap<String, Integer> variableMap = new HashMap();import java.text.*;
import java.util.*;
public class movlangCustomListener extends movlangBaseListener{
	@Override
	public void exitMov1(movlangParser.Mov1Context ctx) {
		if(ctx.REG() != null){
      			System.out.println(ctx.REG().get(0).getText());
    		}
		else {
			System.out.println("Mislukt");
		}
	}
}
